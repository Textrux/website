// packages/formatter/src/formatter.ts
import { Cell } from '@treetsheet/parser';

export function formatCellsToCSV(cells: Cell[][]): string {
	// Convert cells to CSV format
}

export function formatCellsToTSV(cells: Cell[][]): string {
	// Convert cells to TSV format
}
